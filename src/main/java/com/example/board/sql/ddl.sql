CREATE TABLE poster (
                        id bigint not null auto_increment,
                        title varchar(100),
                        username varchar(100),
                        content varchar(300),
                        PRIMARY KEY (id)
);
작성일도 추가해보자.
